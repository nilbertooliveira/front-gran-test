export class ProductDTO{

    constructor(
        id: number,
        name: string,
        price: number,
        photo: string,
        category: string,
        categoryId: number,
        status: boolean
    ) {
    }
}